<div class='top_nav_bar'>
	<h1>MARV'S BLOG</h1>
	<% if user_signed_in? %>
		<%= link_to('Logout', destroy_user_session_path, method: :delete) %>        
	<% else %>
		<%= link_to('Login To Update Blog', new_user_session_path) %>  
	<% end %>
</div>

